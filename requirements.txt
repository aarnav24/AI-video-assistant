# ─────────────────────────────────────────────
# AI Meeting Assistant — requirements.txt
# Python 3.10+ recommended
# ─────────────────────────────────────────────

# ── Audio / Video Download ─────────────────────
yt-dlp>=2024.3.10               # Download audio from YouTube URLs
pydub>=0.25.1                   # Audio processing / format conversion

# ── Speech-to-Text (Groq Whisper API) ──────────
groq>=0.9.0                     # Groq client for Whisper large-v3 API

# ── LLM / LangChain LCEL ───────────────────────
langchain>=0.2.0                # Core LangChain framework (LCEL support)
langchain-community>=0.2.0      # Community integrations
langchain-mistralai>=0.1.0      # LangChain × Mistral AI integration
langchain-text-splitters>=0.2.0 # Text splitting utilities

# ── Embeddings (FastEmbed — lightweight, no torch) ─
fastembed>=0.2.0                # Lightweight embeddings, no torch needed
langchain-chroma>=0.1.0         # LangChain ChromaDB integration

# ── Vector Store (RAG) ─────────────────────────
chromadb>=0.5.0                 # ChromaDB vector store for RAG pipeline

# ── UI (Streamlit) ─────────────────────────────
streamlit>=1.35.0               # Web UI framework

# ── Export (PDF) ───────────────────────────────
fpdf2>=2.7.9                    # Generate PDF exports

# ── Utilities ──────────────────────────────────
python-dotenv>=1.0.1            # Load .env for API keys